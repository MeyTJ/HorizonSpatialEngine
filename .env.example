# PostGIS connection (required for sqlx compile-time verification and runtime queries)
DATABASE_URL=postgres://horizon:horizon@localhost:5432/horizon_spatial

# gRPC listen address
HORIZON_LISTEN_ADDR=0.0.0.0:50051
