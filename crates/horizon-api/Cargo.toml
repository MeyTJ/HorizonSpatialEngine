[package]
name = "horizon-api"
description = "Transport-agnostic service boundary for HorizonSpatialEngine"
publish = false

[lib]
name = "horizon_api"
path = "src/lib.rs"

[dependencies]
thiserror = { workspace = true }
async-trait = "0.1"

[features]
default = []
